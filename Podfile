# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TimeToPost' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TimeToPost
  pod 'ObjectMapper'
  pod 'RealmSwift'
  pod 'Moya'
  pod 'Kingfisher'
  pod 'Quick'
  pod 'Nimble'

  target 'TimeToPostTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Quick'
    pod 'Nimble'
  end

end
